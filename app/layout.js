export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
// Task - API data fetching and state , Fetch remote data and render it in a client-side component . do steps only i ask rule is manually handle the loading states. first step Creates a new page '/dashboard/test-api'. i complete this step one all basics are made i will give the folder structure. 